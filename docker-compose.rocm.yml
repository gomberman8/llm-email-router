services:
  ollama:
    image: ollama/ollama:0.32.5-rocm
    devices:
      - /dev/kfd
      - /dev/dri
    group_add:
      - "${VIDEO_GID:-39}"
      - "${RENDER_GID:-105}"
    security_opt:
      - seccomp:unconfined
